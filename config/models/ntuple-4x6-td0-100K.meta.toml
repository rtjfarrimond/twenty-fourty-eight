name = "ntuple-4x6-td0-100K"
description = "N-tuple network (4 base 6-tuple patterns) trained with TD(0) and afterstate value functions. 100K training games. An early checkpoint showing the model learning."
